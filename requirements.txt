# -------------------------------------------
# Server & API
# -------------------------------------------
fastapi>=0.115,<0.117
uvicorn[standard]>=0.34.0,<0.36.0

# Prometheus metrics for /metrics
prometheus-fastapi-instrumentator==7.1.0
prometheus-client>=0.17.1

# -------------------------------------------
# ACE-Step / audio stack (existing)
# -------------------------------------------
datasets==3.4.1
diffusers>=0.33.0
gradio
librosa==0.11.0
loguru==0.7.3
matplotlib==3.10.1
numpy
pypinyin==0.53.0
pytorch_lightning==2.5.1
soundfile==0.13.1

# NOTE (PyTorch wheels):
# If you install CUDA-enabled wheels separately (recommended),
# comment these three lines out to avoid pip replacing your CUDA build
# with a CPU wheel on some platforms. Your run scripts already install
# the correct Torch wheels for CUDA.
torch
torchaudio
torchvision

tqdm
transformers==4.50.0
py3langid==0.3.0
hangul-romanize==0.1.0
num2words==0.5.14
spacy==3.8.4
accelerate==1.6.0
cutlet
fugashi[unidic-lite]
click
peft
tensorboard
tensorboardX

# -------------------------------------------
# System dependency (not a pip package)
# -------------------------------------------
# FFmpeg must be present on PATH for Opus transcoding (libopus).
# Install via your OS package manager (e.g., apt install ffmpeg / brew install ffmpeg).
